let arr = [1, 2, 3, 5, 7, 9];

// for loop

// for of
for (let i of arr) {
  console.log(arr[i]);
}

// for in
for (let i in arr) {
  console.log(arr[i]);
}

// METHOD
// 1) ARR.SORT
// 2) ARR.REDUCE
//  3) ARR.FILTER
// 4) ARR.MAP

// arr.push()
//arr.pop()
// arr.unshift() ---- adds value at start
// arr.shift() ---- removes value at start

//arr.splice()

//arr.join()
//arr.slice()
//arr.reverse()
